/**
 * @civ/server — placeholder.
 *
 * Skal bli HTTP-laget som ligger over @civ/engine: autentisering, persistens og
 * mapping fra EngineError til statuskoder. Ingenting av det er portert ennå.
 * Gamle motstykker: `resource/*` og `application/*` i old-civ-rest.
 */
export const NOT_IMPLEMENTED = 'server-pakken er ikke startet' as const
