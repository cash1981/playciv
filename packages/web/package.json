{
  "name": "@civ/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(@civ/web: ingen tester ennå)\""
  },
  "dependencies": { "@civ/engine": "workspace:*" },
  "devDependencies": { "typescript": "^5.9.3" }
}
